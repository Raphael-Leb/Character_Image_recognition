#pragma once

#include "ModuleConstantes.h"

/* Procédure qui charge la première image de test qui correspond au chiffre 7. */
void chargerImageTest1(unsigned char** img);

/* Procédure qui charge la deuxième image de test qui correspond au chiffre 2. */
void chargerImageTest2(unsigned char** img);